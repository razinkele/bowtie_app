# =============================================================================
# Environment-Specific Configuration Override
# =============================================================================
# This file allows you to override settings in config.R for different
# environments (development, testing, production) without modifying config.R
# 
# USAGE:
# 1. Copy this file and remove the .example extension: cp .env.R.example .env.R
# 2. Uncomment and modify the settings you want to override
# 3. The .env.R file is gitignored and won't be committed
# =============================================================================

# Example environment-specific overrides
ENV_CONFIG <- list(
  # Override application name for testing
  # APP_NAME = "bowtie_app_test",
  
  # Override version for development
  # VERSION = "5.2.0-dev",
  
  # Enable debug mode in development
  # DEV = list(
  #   DEBUG_MODE = TRUE,
  #   ENABLE_PROFILING = TRUE,
  #   SHOW_ERRORS = TRUE,
  #   RELOAD_ON_SAVE = TRUE
  # ),
  
  # Use different port for development
  # DEFAULT_PORT = 8080,
  
  # Override data file paths for testing
  # DATA_FILES = list(
  #   CAUSES = "test_data/CAUSES_test.xlsx",
  #   CONSEQUENCES = "test_data/CONSEQUENCES_test.xlsx",
  #   CONTROLS = "test_data/CONTROLS_test.xlsx"
  # ),
  
  # Enable logging with debug level
  # LOGGING = list(
  #   ENABLED = TRUE,
  #   LEVEL = "DEBUG",
  #   FILE = "debug.log",
  #   MAX_SIZE_MB = 100
  # ),
  
  # Disable caching for development
  # PERFORMANCE = list(
  #   ENABLE_CACHING = FALSE,
  #   CACHE_DIR = "app_cache_dev",
  #   ENABLE_PROGRESS_BAR = TRUE,
  #   DEBOUNCE_MS = 100
  # ),
  
  # Enable database for production
  # DATABASE = list(
  #   ENABLED = TRUE,
  #   TYPE = "postgres",
  #   HOST = "localhost",
  #   PORT = 5432,
  #   NAME = "bowtie_app",
  #   USER = Sys.getenv("DB_USER"),
  #   PASSWORD = Sys.getenv("DB_PASSWORD")
  # ),
  
  # Production deployment path
  # SHINY_SERVER_DIR = "/srv/shiny-server"
)

# Print override notice
cat("🔧 Environment-specific configuration loaded\n")
cat("   • Overrides applied:", length(ENV_CONFIG), "settings\n")
